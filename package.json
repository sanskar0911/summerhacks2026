{
  "name": "agentos-root",
  "private": true,
  "version": "1.0.0",
  "description": "Root config to build frontend on Vercel",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  }
}
